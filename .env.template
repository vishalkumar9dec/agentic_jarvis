# Agentic Jarvis - Environment Configuration Template
# Copy this file to .env and fill in your values

# ============================================================================
# Core Configuration
# ============================================================================

# Google API Key (required)
# Get from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# ============================================================================
# Service Ports
# ============================================================================

# Tickets Toolbox Server
TICKETS_SERVER_PORT=5001

# FinOps Toolbox Server
FINOPS_SERVER_PORT=5002

# Oxygen A2A Agent
OXYGEN_AGENT_PORT=8002

# Authentication Service (Phase 2)
AUTH_SERVICE_PORT=9998

# Web UI Server
WEB_UI_PORT=9999

# ============================================================================
# Phase 2: JWT Authentication
# ============================================================================

# JWT Secret Key for token signing
# IMPORTANT: Change this in production to a strong, random secret
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-secret-key-change-in-production

# JWT Algorithm (HS256 recommended for symmetric signing)
JWT_ALGORITHM=HS256

# JWT Token Expiration (hours)
JWT_EXPIRATION_HOURS=24

# Auth Service URL
AUTH_SERVICE_URL=http://localhost:9998

# ============================================================================
# Phase 3: Session/Memory (Future)
# ============================================================================

# Session storage type: memory | database | redis
# SESSION_TYPE=memory

# Database URL for session persistence
# DATABASE_URL=sqlite:///./jarvis.db

# ============================================================================
# Phase 4: OAuth 2.0 (Future)
# ============================================================================

# OAuth Provider: google | azure | auth0 | okta
# OAUTH_PROVIDER=google

# OAuth Client Credentials
# OAUTH_CLIENT_ID=your_client_id
# OAUTH_CLIENT_SECRET=your_client_secret

# ============================================================================
# Notes
# ============================================================================

# Phase 1: Core functionality (no auth)
# Phase 2: JWT authentication (current)
# Phase 3: Memory & session persistence
# Phase 4: OAuth 2.0 & enterprise SSO
