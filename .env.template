# =============================================================================
# Agentic Jarvis - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
#
# IMPORTANT: Never commit .env to version control!
# The .env file contains sensitive secrets and API keys.
#
# Quick Start:
#   1. cp .env.template .env
#   2. Get Google API key: https://makersuite.google.com/app/apikey
#   3. Generate JWT secret: ./scripts/generate_jwt_secret.sh
#   4. Update GOOGLE_API_KEY and JWT_SECRET_KEY in .env
# =============================================================================

# -----------------------------------------------------------------------------
# Google API Key (Required)
# -----------------------------------------------------------------------------
# Get your API key from: https://makersuite.google.com/app/apikey
# Used by all agents for LLM calls (Gemini 2.5 Flash)
GOOGLE_API_KEY=your_google_api_key_here

# -----------------------------------------------------------------------------
# Service Ports (Phase 1: Pure A2A Architecture)
# -----------------------------------------------------------------------------
# Agent services (self-contained A2A services)
TICKETS_AGENT_PORT=8080        # IT operations ticket management
FINOPS_AGENT_PORT=8081         # Cloud cost analytics
OXYGEN_AGENT_PORT=8082         # Learning & development platform

# Registry and session service
REGISTRY_SERVICE_PORT=8003     # Agent registry + session management

# Legacy ports (Phase 0 - Deprecated, kept for backward compatibility)
TICKETS_SERVER_PORT=5001       # Old toolbox server (deprecated)
FINOPS_SERVER_PORT=5002        # Old toolbox server (deprecated)

# Web UI and Authentication
AUTH_SERVICE_PORT=9998         # JWT authentication service
WEB_UI_PORT=9999              # Web interface (if implemented)

# -----------------------------------------------------------------------------
# Phase 2: JWT Authentication (Active)
# -----------------------------------------------------------------------------
# JWT Secret Key for token signing
# IMPORTANT: NEVER use the default value in production!
# Generate a secure secret with: ./scripts/generate_jwt_secret.sh
# Keep this secret secure - never commit it to version control
JWT_SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# JWT Algorithm
# Phase 2: HS256 (symmetric key, simpler setup)
# Phase 4: RS256 (asymmetric key, for OAuth 2.0)
JWT_ALGORITHM=HS256

# JWT Token Expiration (hours)
# Tokens expire after this duration for security
# Default: 24 hours (1 day)
JWT_EXPIRATION_HOURS=24

# Auth Service URL
# Used by CLI and web UI to authenticate users
AUTH_SERVICE_URL=http://localhost:9998

# -----------------------------------------------------------------------------
# Phase 3: Session & Memory (Future)
# -----------------------------------------------------------------------------
# Uncomment when implementing Phase 3
#
# Session storage type
# SESSION_TYPE=memory              # Options: memory | database | redis
#
# Database URL for session persistence
# DATABASE_URL=sqlite:///./jarvis.db
#
# Vector DB for long-term memory
# VECTOR_DB_TYPE=chromadb          # Options: chromadb | pinecone | weaviate
# CHROMA_PERSIST_DIRECTORY=./data/chroma

# -----------------------------------------------------------------------------
# Phase 4: OAuth 2.0 (Future)
# -----------------------------------------------------------------------------
# Uncomment when implementing Phase 4
#
# OAuth Provider
# OAUTH_PROVIDER=google            # Options: google | azure | okta | auth0
#
# OAuth Client Credentials (from your OAuth provider)
# OAUTH_CLIENT_ID=your_client_id
# OAUTH_CLIENT_SECRET=your_client_secret
# OAUTH_REDIRECT_URI=http://localhost:9999/callback
#
# For Azure AD
# AZURE_TENANT_ID=your_tenant_id
#
# For Okta
# OKTA_DOMAIN=your-domain.okta.com

# -----------------------------------------------------------------------------
# Development & Debugging (Optional)
# -----------------------------------------------------------------------------
# LOG_LEVEL=INFO                   # Options: DEBUG | INFO | WARNING | ERROR
# ENABLE_DEBUG_MODE=false
# ENABLE_TELEMETRY=false

# -----------------------------------------------------------------------------
# Production Settings (Future)
# -----------------------------------------------------------------------------
# ENVIRONMENT=development          # Options: development | staging | production
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:9999
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_REQUESTS_PER_MINUTE=60

# =============================================================================
# Notes
# =============================================================================
# Phase 1: Core multi-agent functionality (complete)
# Phase 2: JWT authentication + session persistence (complete)
# Phase 3: Long-term memory & proactive notifications (future)
# Phase 4: OAuth 2.0 & enterprise SSO (future)
#
# Helpful Commands:
#   - Generate JWT secret: ./scripts/generate_jwt_secret.sh
#   - Check services: ./scripts/check_services.sh
#   - Start all services: ./scripts/start_all_agents.sh
#   - Documentation: docs/PHASE_2_PURE_A2A_PLAN.md
# =============================================================================
